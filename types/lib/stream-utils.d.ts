export class StreamUtils {
    static streamToBuffer(stream: any): Promise<any>;
}
export class ReadableStreamClone extends Readable {
    constructor(readableStream: any);
    _read: () => void;
}
import Readable_1 = require("stream");
import Readable = Readable_1.Readable;
